Installation

afin de lancer l'application il vous faut :

- Executé la commande "npm install" dans la console depuis le dossier frontend ET backend.
- Depuis le dossier backend exécuté la commande "nodemon server".
- Depuis le dossier frontend exécuté la commande "npm start", "npm install react".